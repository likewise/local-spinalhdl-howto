# Local SpinalHDL build, with VexRiscv build

On a clean system (I am using a freshly started Docker container), try
to build SpinalHDL v1.6.4, then build VexRiscv against that.

Currently does not build.
